/** Protein prediction visualization workstation (formerly Gaster / /workspace). */
export const HELIX_PATH = "/helix";

/** Drug interaction analysis workstation (formerly Helix DDI). */
export const PHAELEON_PATH = "/phaeleon";

/** Bioengineering-specialized AI chat home. */
export const BINARY_PATH = "/binary";

/** Layer composer — P0 hub card only (no page yet). */
export const STRATA_PATH = "/strata";

/** Docking prep — P0 hub card only (no page yet). */
export const DOCKYARD_PATH = "/dockyard";

/** Scientific metrics HUD — P0 hub card only (no page yet). */
export const PULSE_PATH = "/pulse";

/** Legacy route — redirects to Helix in App router. */
export const LEGACY_GASTER_PATH = "/gaster";

/** Legacy route — redirects to Helix in App router. */
export const LEGACY_WORKSPACE_PATH = "/workspace";
